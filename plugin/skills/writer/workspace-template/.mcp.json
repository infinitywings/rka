{
  "_comment": "Portable Writer MCP configuration. Project scope is explicit on every RKA operation; no default-project environment variable or credentials are stored in the workspace.",

  "mcpServers": {
    "rka": {
      "command": "rka",
      "args": ["mcp"],
      "env": {
        "RKA_API_URL": "REPLACE_WITH_RKA_API_URL"
      }
    },
    "rka-writer-tools": {
      "command": "rka-writer-tools",
      "args": []
    }
  }
}
