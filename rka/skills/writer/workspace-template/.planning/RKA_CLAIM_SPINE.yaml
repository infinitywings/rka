# RKA_CLAIM_SPINE.yaml
#
# Read-only Writer projection from the canonical native RKA manuscript.
# Replace this bootstrap placeholder with `rka writer sync`. Do not edit this
# file as a source of truth; use dry-run/apply native spine commands.

schema_version: rka-claim-spine/v2
authoritative_source: rka
project_id: REPLACE_WITH_PROJECT_ID
manuscript_id: REPLACE_WITH_MANUSCRIPT_ID
manuscript_revision: null
changelog_cursor: null

# Populated only by `rka writer sync`.
claims: []

# Populated only by `rka writer sync`.
units: []

# Populated only by `rka writer sync`. `verify_citations.py` derives approval
# from member-level validation.current and rejects this unsynchronized
# placeholder.
reference_manifest:
  schema_version: rka.manuscript-reference-manifest/v1
  authoritative_source: rka
  project_id: REPLACE_WITH_PROJECT_ID
  manuscript_id: REPLACE_WITH_MANUSCRIPT_ID
  manuscript_revision: null
  members: []
  active_citation_keys: []
  approved_citation_keys: []
  all_members_verified: false
