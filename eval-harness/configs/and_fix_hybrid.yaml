# T2 fills this with the AND-fix + hybrid (FTS + semantic) config:
#   RKA_FTS_QUERY_MODE: and         # applies the Mission A revision-report fix
#   RKA_EMBEDDINGS_ENABLED: true    # FTS + semantic hybrid
#   top_k: 10
#   ...
