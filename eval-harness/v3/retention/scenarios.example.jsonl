# Example scenario with SYNTHETIC content. Real corpora should plant items drawn from a live project (and ingest them for the rka arm), use topically-adjacent filler, and probe at 10k/50k/150k-token distances.
{"scenario_id": "example-detector-thresholds", "seeded_items": [{"item_id": "dir-ci", "kind": "directive", "text": "PI directive: every reported detection metric MUST include its 95% confidence interval, and results from the retracted DelayNet-v1 run must never be cited."}, {"item_id": "ev-auc", "kind": "evidence", "text": "Finding clm_01EXAMPLEAUCRESULT000000: the detector achieves 0.73 AUC at the 25ms aggregation window; at 50ms it degrades to 0.61."}], "filler_tasks": [{"prompt": "Summarize the related work on CPS anomaly detection latency.", "canned_response": "The related work spans statistical residual detectors, learned observers, and hybrid schemes. Latency budgets differ across plants... (filler content standing in for a long working session; real corpora use several thousand tokens of topically-adjacent work here so retrieval baselines are fairly stressed)."}, {"prompt": "Draft the experiment matrix for the ablation over aggregation windows.", "canned_response": "Ablation axes: aggregation window in {10ms, 25ms, 50ms, 100ms}, sensor subset, attack family. Each cell runs 5 seeds... (more filler)."}], "probes": [{"probe_id": "p1-early-evidence", "target_item": "ev-auc", "after_tokens": 100, "prompt": "What AUC did the detector achieve at the 25ms window? Cite the source entity.", "expect": {"numeric": {"value": 0.73, "tolerance": 0.001}, "expected_citations": ["clm_01EXAMPLEAUCRESULT000000"]}}, {"probe_id": "p2-late-directive", "target_item": "dir-ci", "after_tokens": 300, "prompt": "Write one sentence reporting the 25ms detection result for the paper.", "expect": {"must_include": ["confidence interval"], "must_not_include": ["DelayNet-v1"]}}]}
