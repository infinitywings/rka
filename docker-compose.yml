# =============================================================
# RKA — Research Knowledge Agent
# One-click deployment: docker compose up -d
# =============================================================
#
# Prerequisites:
#   - Docker Desktop (macOS/Windows) or Docker Engine (Linux)
#   - LM Studio running on host at http://localhost:1234
#     (or Ollama — change LLM env vars below)
#
# Usage:
#   docker compose up -d          # start
#   docker compose logs -f rka    # view logs
#   docker compose down           # stop
#
# Web UI:  http://localhost:9712
# API:     http://localhost:9712/docs
#
# Claude Desktop MCP config (~/.config/Claude/claude_desktop_config.json):
#   {
#     "mcpServers": {
#       "rka": {
#         "command": "docker",
#         "args": ["exec", "-i", "rka-server", "rka", "mcp"]
#       }
#     }
#   }
# =============================================================

services:
  rka:
    build: .
    container_name: rka-server
    ports:
      - "9712:9712"
    volumes:
      - rka-data:/data
    environment:
      # --- LLM Backend ---
      # MiniMax API (production)
      RKA_LLM_API_BASE: https://api.minimax.io/v1
      RKA_LLM_MODEL: openai/MiniMax-M2.7
      RKA_LLM_API_KEY: sk-cp-zgGbIn29oh8N0H4ZCz_QutnNEPkoWxACLXTxIOSeOv4-sM8bPYK-NmvJfZZQOh03YJ7-VHJ9eTT3HzxrgmOT3TxK7rlwA-u2xEs1ebcxxIZErCSUj1u7QuM
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
  rka-worker:
    build: .
    container_name: rka-worker
    command: ["rka", "worker"]
    healthcheck:
      disable: true
    volumes:
      - rka-data:/data
    environment:
      RKA_LLM_API_BASE: https://api.minimax.io/v1
      RKA_LLM_MODEL: openai/MiniMax-M2.7
      RKA_LLM_API_KEY: sk-cp-zgGbIn29oh8N0H4ZCz_QutnNEPkoWxACLXTxIOSeOv4-sM8bPYK-NmvJfZZQOh03YJ7-VHJ9eTT3HzxrgmOT3TxK7rlwA-u2xEs1ebcxxIZErCSUj1u7QuM
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - rka
    restart: unless-stopped

volumes:
  rka-data:
