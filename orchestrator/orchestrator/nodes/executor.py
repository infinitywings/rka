"""Executor nodes (3).

Scaffold stub. T4 will implement:

  - backbrief_draft   — produce upfront Backbrief journal note
  - mission_execute   — perform the mission work + record artifacts
  - submit_report     — `rka_submit_report` wrapper with provenance
"""

from __future__ import annotations


def backbrief_draft(state):  # pragma: no cover
    raise NotImplementedError("executor.backbrief_draft arrives in T4")


def mission_execute(state):  # pragma: no cover
    raise NotImplementedError("executor.mission_execute arrives in T4")


def submit_report(state):  # pragma: no cover
    raise NotImplementedError("executor.submit_report arrives in T4")
