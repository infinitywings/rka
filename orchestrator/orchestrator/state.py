"""Workflow state schema.

Scaffold stub — full TypedDict + reducers land in T2 (state schema task)
which triggers the mid-mission Backbrief gate before T3 nodes are built.
"""

from __future__ import annotations

from typing import TypedDict


class ResearchWorkflowState(TypedDict, total=False):
    """Placeholder. Real fields + reducers arrive in T2."""

    thread_id: str
